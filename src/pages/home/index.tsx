import Hero from './components/hero-section';

const Home = () => {
  return (
    <main className="fixed-width flex items-center justify-center">
      <Hero />
    </main>
  );
};

export default Home;
